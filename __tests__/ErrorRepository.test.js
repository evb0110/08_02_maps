import ErrorRepository from '../src/js/ErrorRepository';

const errorRepository = new ErrorRepository;
const errors = ['Type Error', 'Reference Error', 'Good Error', 'Bad Error', ]

test('test 0, should return Type Error', () => {
});